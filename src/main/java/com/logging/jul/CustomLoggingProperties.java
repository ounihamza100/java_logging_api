package com.logging.jul;

/**
 * {@link java.util.logging.LogManager }  is the class that reads the logging configuration, create and maintains the logger instances.
 * We can use this class to set our own application specific configuration.
 *
 * If we don’t specify any configuration, it’s read from JRE Home {@ lib/logging.properties file}.
 * @author Hamza Ouni
 */
public class CustomLoggingProperties {
}
